alias dnf="dnf5"
alias sdnf="sudo dnf5 install"

# this stuff bugs out a lot so i don't use it but it's there for funnies
alias b='sudo "$BASH" -c "$(history -p !!)"'
eval $(thefuck --alias)
eval $(thefuck --alias FUCK)
eval $(thefuck --alias cfix)