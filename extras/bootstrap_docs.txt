`sudo dnf upgrade --refresh` (then reboot)
`sudo dnf install python3-pip ansible` (ansible deps)
`~/dotfiles/scripts/dotfiles-link`
`dreset` for cinnamon options
`~/dotfiles/scripts/sap` (run ansible playbook)
`sudo hostnamectl set-hostname dregsdesk` (set hostname)
`~.dotfiles/scripts/updscr` to update software
extra packages
    gearlever
        musescore
        librescore
            https://github.com/LibreScore/app-librescore
            LibreScore.AppImage
    rpm files
        vesktop (auto-update)
    virt-manager ('@virtualization' group in fedora)
        https://www.tomshardware.com/how-to/install-windows-11-without-microsoft-account
            (shift+F10 cmd, `start ms-cxh:localonly`)
            or google more recent instructions
        https://www.debugpoint.com/kvm-share-folder-windows-guest/
            `choco install winfsp` for winfsp
github auth with `gh auth login`
sync stuff/settings
    vivaldi
    vs code
    vesktop
Vivaldi stuff:
    sync
    Violentmonkey
        dl-librescore
        bypass-all-shortlinks-debloated
    search engines
    other extensions
qalculate-gtk: Mode > Rational Number Form > Exact Decimal Fractions
