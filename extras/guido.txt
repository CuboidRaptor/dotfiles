`yippee -b` (ansible/dotfiles/distrobox bootstrapping)
extra packages
    gearlever
        musescore
        librescore
    rpm files
        vesktop
    virt-manager ('@virtualization' group in fedora)
        https://www.tomshardware.com/how-to/install-windows-11-without-microsoft-account
            (shift+F10 cmd, `start ms-cxh:localonly`)
            or google more recent instructions
        https://www.debugpoint.com/kvm-share-folder-windows-guest/
            `choco install winfsp` for winfsp
github auth with `gh auth login`
celeste https://github.com/EverestAPI/Resources/wiki/common-crashes#systemcomponentmodelwin32exception-selinux-execheap-probe-failed-please-ensure-everest-has-this-permission-then-try-again
sync stuff/settings
    vivaldi
    vs code
    vesktop
Vivaldi stuff:
    sync
    enable middle click autoscrroll
        `cp "/usr/share/applications/vivaldi-stable.desktop" "./dotfiles/.local/share/applications/vivaldi-stable.desktop"`
        run this command from DOTFILES ROOT
        add `--enable-blink-features=MiddleClickAutoscroll` after all `/usr/bin/vivaldi-stable`
    Violentmonkey
        dl-librescore
        bypass-all-shortlinks-debloated
    search engines
    xkcd-ntmin
Browser/firefox stuff:
    search engines
    Violentmonkey
        librescore
        bypass all shortlinks debloated
    uBO
        enable cloud sync support
        manually load/sync settings
    return youtube dislike
    the other stuff (xkcd-ntmin)
other setup notes:
    mark packages as explicitly installed:
        `sudo dnf mark user`
        `sudo pacman -D --asexplicit`
    update firmware
        `sudo fwupdmgr get-updates && sudo fwupdmgr upgrade`
